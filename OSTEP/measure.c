#include <stdio.h>
#include <stdlib.h>
#include <time.h>

// Measure the costs of system call and context switch

int measureContextSwitch()
{
    // One difficulty in measuring context-switch cost arises in systems with
    // more than one CPU; what you need to do on such a system is ensure that
    // your context-switching processes are located on the same processor.


    return 0;
}


int measureCostOfSystemCall()
{
    return 0;
}


int main(int argc, char *argv[])
{
    printf("Measurement Homeworks\n\n");
    printf("Code that measures the cost of system calls and context switch\n");

    for(int i = 0; i < 10; i++) {
        int currTime = gettime
    }
    
    return 0;
}
